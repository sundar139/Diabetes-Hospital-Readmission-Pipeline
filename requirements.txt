# Lightweight runtime for Streamlit deployment and frontend CI smoke checks.
# Full local development/training dependencies remain in pyproject.toml via uv.
# pydantic / pydantic-settings are intentionally excluded from this path.
streamlit>=1.44.0
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
xgboost>=2.1.0
joblib>=1.4.0
